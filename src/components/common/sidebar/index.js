import React from 'react';
import Navbar from '../header/navbar'

const SideBar = (props) => {
    return (
        <nav className="col-3 nav navbar-expand-md">
            <Navbar/>
        </nav>
    )
}

export default SideBar;